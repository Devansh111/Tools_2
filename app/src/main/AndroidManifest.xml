<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.devansh.tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.home"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.toolbox"
            android:label="@string/title_activity_toolbox"
            android:parentActivityName=".Activities.home"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Activities.home" />
        </activity>
        <activity
            android:name=".Activities.messages"
            android:label="@string/title_activity_messages"
            android:parentActivityName=".Activities.home"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.devansh.tools.Activities.home" />
        </activity>
        <activity
            android:name=".Activities.profile"
            android:label="@string/title_activity_profile"
            android:parentActivityName=".Activities.home"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.devansh.tools.Activities.home" />
        </activity>
    </application>

</manifest>